# Core
torch>=2.2.0
torchaudio>=2.2.0
transformers>=4.40.0
accelerate>=0.29.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.26.0
PyYAML>=6.0
av>=13.0.0   # ingest.py video/m4a/broadcast-container decode path (_load_audio_av)

# ASR engines
faster-whisper>=1.2.0   # Engine A default (CTranslate2) — needs a CT2-converted model:
                        #   ct2-transformers-converter --model biodatlab/whisper-th-medium-combined \
                        #     --output_dir models/whisper-th-medium-ct2 --quantization float16 \
                        #     --copy_files tokenizer.json preprocessor_config.json
funasr>=1.1.0
openai-whisper>=20250625
# Engine B candidate (Typhoon RT / FastConformer-Transducer). Heavy C-dep tree
# (pytorch-lightning, optuna, wandb, ...) — kept commented out of the default
# install. VERIFIED 2026-07-16: installs and imports cleanly on this 3.11.9
# venv (nemo_toolkit==2.7.3) — the old "Python 3.13 wheel UNVERIFIED" note no
# longer applies. Probed as engine_b and REJECTED on accuracy: regresses
# CER_thai and WER_latin vs both the passthrough baseline and funasr, with
# only a marginal BER edge. Don't re-try without new evidence; see TODO_LEDGER.
# nemo_toolkit[asr]>=2.7.0

# Thai NLP
pythainlp>=5.0.0
python-crfsuite>=0.9  # pythainlp's crfcut sentence tokenizer (unpunctuated Thai text)

# VAD
silero-vad>=5.1   # pip package — no torch.hub network fetch / supply-chain surface
                  # (falls back to torch.hub if this package is absent)

# Denoising
deepfilternet>=0.5.6

# Web editor
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Eval
jiwer>=3.0.0
rapidfuzz>=3.0.0   # C Levenshtein for eval metrics (~100× the pure-Python fallback)
