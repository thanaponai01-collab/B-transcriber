# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Database
*.db
*.sqlite3

# pytest
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.suo
*.user

# OS
.DS_Store
Thumbs.db

# Model weights / large files
# CT2-converted engines, regenerated via ct2-transformers-converter
models/
*.bin
*.pt
*.pth
*.gguf
*.safetensors

# Logs
*.log

# Transcript exports
output/

# Gold-set source audio/video: keep local only (size + provenance), alongside
# the .json ground truth they belong to, which stays tracked.
transcribe/eval/goldenset/*.mp4
transcribe/eval/goldenset/*.mp3
transcribe/eval/goldenset/*.wav
transcribe/eval/goldenset/*.flac

# Fine-tune data engine output (Phase C, tools/make_finetune_set.py /
# tools/finetune_lora.py): sliced training audio and LoRA checkpoints are
# user-supplied/regenerated, not tracked. manifest.jsonl (the text index of
# what's been collected) stays tracked - it's small and useful provenance.
transcribe/finetune/data/
transcribe/finetune/lora_out/

# Environment
.env
.env.*
